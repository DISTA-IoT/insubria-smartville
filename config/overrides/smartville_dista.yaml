
health_monitoring: true

intrusion_detection:
  pretrained_inference: false
  epistemic_agency: false
  online_evaluation: true
  report_step_freq: 10
  inference_freq_secs: 1
  flowstats_freq_secs: 5

  
topology_creator:
  use_gns3_config_file: false
  ADDITIONAL_ENV_VARS: |
    https_proxy=http://proxy.uninsubria.it:3128
    HTTPS_PROXY=http://proxy.uninsubria.it:3128
    HTTP_PROXY=http://proxy.uninsubria.it:3128
    http_proxy=http://proxy.uninsubria.it:3128
    no_proxy=localhost,127.0.0.1


base_params: 
  host_ip: 192.168.183.146
  # container_manager_replay_from_file : false
  disable_proxy: true
  replay_speed: 1 # Replay speed factor


honeypots:
  - victim-0:
      pattern: 'doorlock'
      destination: 'victim-3'
  - victim-1:
      pattern: 'echo'
      destination: 'victim-4'
  - victim-2:
      pattern: 'hue'
      destination: 'victim-5'
  - victim-3:
      pattern: 'doorlock'
      destination: 'victim-1'
  - victim-4:
      pattern: 'echo'
      destination: 'victim-2'
  - victim-5:
      pattern: 'hue'
      destination: 'victim-0'


attackers:
  - attacker-1:
      pattern: 'cc_heartbeat'
      destination: 'victim-0'
  - attacker-2:
      pattern: 'generic_ddos'
      destination: 'victim-1'
  - attacker-3:
      pattern: 'h_scan'
      destination: 'victim-2'
  - attacker-4:
      pattern: 'hakai'
      destination: 'victim-3'
  - attacker-5:
      pattern: 'torii'
      destination: 'victim-4'
  - attacker-6:
      pattern: 'mirai'
      destination: 'victim-5'
  - attacker-7:
      pattern: 'gafgyt'
      destination: 'victim-0'
  - attacker-8:
      pattern: 'hajime'
      destination: 'victim-1'
  - attacker-9:
      pattern: 'okiru'
      destination: 'victim-2'
  - attacker-10:
      pattern: 'muhstik'
      destination: 'victim-3'
  - attacker-11:
      pattern: 'doorlock'
      destination: 'victim-1'
  - attacker-12:
      pattern: 'echo'
      destination: 'victim-2'
  - attacker-13:
      pattern: 'hue'
      destination: 'victim-0'